# Garbage Tests

These tests cover old unfinished, deprecated, or ignored garbage-drawer surfaces and supporting logic.

Do not read or run these tests for normal active-surface work. Use them only when the user explicitly asks for a garbage-drawer screen or garbage logic.

Do not add new tests here without the user's explicit agreement and a plain-English explanation of what behavior the test protects.
